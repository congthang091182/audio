# Core requirements (đã kiểm tra kỹ tương thích)
streamlit==1.32.2
pandas==2.1.4
pyodbc==5.1.0
plotly==5.18.0
openpyxl==3.1.2
reportlab==4.0.4
python-dotenv==1.0.0
uuid==1.30

# Cookie Manager alternative (phiên bản đã xác nhận tồn tại)
extra-streamlit-components==0.1.71

# Windows-specific
pywin32==305; sys_platform == 'win32'

# Các dependencies phụ
python-dateutil==2.8.2
typing-extensions==4.9.0
tzdata==2023.4; sys_platform == 'win32'

# Font thay thế cho ReportLab (thay vì reportlab-fonts)
#xhtml2pdf==0.2.11  # Bao gồm font mặc định tốt